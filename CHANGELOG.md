# Changelog

## [1.1.0]

### Added

- 新增 QQ OneBot (`aiocqhttp`) 合并转发消息审核：通过 `get_forward_msg` 递归读取转发节点的文本和图片，并纳入现有检测流程。
- 新增 QQ 合并转发节点图片审核：读取 OneBot 图片段的 `url` 或 `file`，复用现有视觉 Provider 和 OCR 文字检测流程。
- 新增 `qq_forward_debug` 配置项。开启后会在 AstrBot 日志中记录 QQ 合并转发 ID、节点发送者和解析文本，便于确认协议端返回内容。
- 为 QQ 合并转发增加递归深度、组件深度和请求次数限制，避免异常嵌套或大量节点影响消息处理。
- `敏感词 批量发送` 现在会在本批 AI 审核结束后返回审核总数和命中数；跳过、缺少 Provider 和调用失败也会返回明确状态。
- 新增 QQ 合并转发、群级总开关覆盖、批量队列配置变更和 LLM 批量越界索引的回归测试。

### Changed

- QQ 合并转发命中后的撤回、警告和通知对象固定为发送转发卡的当前用户；转发节点原发送者仅用于调试日志。
- 启用 AI 批量审核时，一整张 QQ 合并转发卡现在作为一条消息进入批量队列；节点文本会保留边界后合并送审，不再逐节点即时调用 AI。
- 完善 README 的跨平台转发/引用说明，以及 QQ 合并转发的调试模式、图片审核和批量审核边界说明。
- 批量 AI 审核队列在发送前重新检查当前访问控制、插件总开关和本群 AI 审核开关。排队期间被排除或关闭审核的消息会被丢弃，不再送审或处罚。

### Fixed

- 调试模式现在会输出 QQ 合并转发节点的原始数据及未解析原因，便于适配不同 OneBot 协议端返回的节点格式。
- 修复 NapCat 将合并转发节点返回为完整 OneBot `message` 事件时，插件只读取 `content` 而漏检节点文本的问题。
- 修复全局插件总开关关闭时，群级“开启”覆盖无法生效的问题。群级配置现在正确优先于全局默认值。
- 修复 LLM 批量审核接受负数或越界 `index` 的问题，防止异常返回伪造完整结果并导致静默漏检。
- 删除配置 schema 中重复的 `image_detection` 定义；现有配置字段和行为保持兼容。

### Limitations

- QQ 合并转发会审核节点文本和图片，但文件、语音、视频等其他媒体内容当前不会展开审核；图片审核还依赖协议端提供视觉 Provider 可读取的 `url` 或 `file`。
- 飞书的引用回复、以及 Slack、钉钉、企业微信中事件未直接携带的历史转发内容，当前不保证可以取得或审核。
